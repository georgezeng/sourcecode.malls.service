spring:
  liquibase:
    enabled: false
  redis:
    host: localhost
    port: 6379
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    table:
      prefix: 
    druid:
      driver-class-name: org.h2.Driver
      url: jdbc:h2:mem:master;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      username: sa
      password: 

access.control.allow.origin: http://localhost:8080

user.type.name: 

super-admin:
  username: admin
  password: 111111
  email: tt@test.com

host:
  database: localhost
  
aliyun:
  oss:
    endpoint: oss-cn-shenzhen.aliyuncs.com
    privateBucket: malls-admin-private-local
    publicBucket: malls-admin-public-local

alipay:
  api:
    url:
      pay:
        notify: